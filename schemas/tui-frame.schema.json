{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.ismail-elkorchi.dev/terminal-ui/tui-frame.schema.json",
  "type": "object",
  "required": ["schemaVersion", "width", "height", "widthProfile", "cells", "accessibility"],
  "properties": {
    "schemaVersion": { "const": "terminal-ui.tui-frame.v1" },
    "width": { "type": "integer", "minimum": 0 },
    "height": { "type": "integer", "minimum": 0 },
    "widthProfile": { "$ref": "#/$defs/textWidthProfile" },
    "cells": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["row", "column", "text", "width"],
        "properties": {
          "row": { "type": "integer", "minimum": 1 },
          "column": { "type": "integer", "minimum": 1 },
          "text": { "type": "string" },
          "width": { "type": "integer", "minimum": 0 },
          "style": { "$ref": "#/$defs/terminalStyle" },
          "link": { "$ref": "#/$defs/terminalLink" },
          "source": { "$ref": "#/$defs/frameCellSource" },
          "continuation": { "type": "boolean" }
        },
        "additionalProperties": false
      }
    },
    "metadata": { "$ref": "#/$defs/frameBufferSnapshotMetadata" },
    "hitTargets": {
      "type": "array",
      "items": { "$ref": "#/$defs/frameHitTarget" }
    },
    "cursor": {
      "type": "object",
      "required": ["row", "column"],
      "properties": {
        "row": { "type": "integer", "minimum": 1 },
        "column": { "type": "integer", "minimum": 1 },
        "style": { "$ref": "#/$defs/terminalStyle" },
        "source": { "$ref": "#/$defs/frameCellSource" }
      },
      "additionalProperties": false
    },
    "focusPath": { "type": "array", "items": { "type": "string" } },
    "accessibility": { "$ref": "https://schemas.ismail-elkorchi.dev/terminal-ui/accessible-snapshot.schema.json" }
  },
  "$defs": {
    "textWidthProfile": {
      "type": "object",
      "required": ["emoji", "ambiguous"],
      "properties": {
        "emoji": { "enum": ["narrow", "wide"] },
        "ambiguous": { "enum": ["narrow", "wide"] }
      },
      "additionalProperties": false
    },
    "terminalStyle": {
      "type": "object",
      "properties": {
        "fg": { "$ref": "#/$defs/terminalColor" },
        "bg": { "$ref": "#/$defs/terminalColor" },
        "bold": { "type": "boolean" },
        "dim": { "type": "boolean" },
        "italic": { "type": "boolean" },
        "underline": { "type": "boolean" },
        "strikethrough": { "type": "boolean" },
        "inverse": { "type": "boolean" },
        "hidden": { "type": "boolean" }
      },
      "additionalProperties": false
    },
    "terminalColor": {
      "oneOf": [
        {
          "type": "object",
          "required": ["kind", "value"],
          "properties": {
            "kind": { "const": "ansi" },
            "value": { "type": "integer", "minimum": 0, "maximum": 255 }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": ["kind", "r", "g", "b"],
          "properties": {
            "kind": { "const": "rgb" },
            "r": { "type": "integer", "minimum": 0, "maximum": 255 },
            "g": { "type": "integer", "minimum": 0, "maximum": 255 },
            "b": { "type": "integer", "minimum": 0, "maximum": 255 }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": ["kind", "token"],
          "properties": {
            "kind": { "const": "theme" },
            "token": { "type": "string", "minLength": 1 }
          },
          "additionalProperties": false
        }
      ]
    },
    "terminalLink": {
      "type": "object",
      "required": ["href"],
      "properties": {
        "href": { "type": "string" },
        "id": { "type": "string" }
      },
      "additionalProperties": false
    },
    "frameCellSource": {
      "type": "object",
      "properties": {
        "ownerId": { "type": "string" },
        "ownerKind": { "type": "string" },
        "family": { "type": "string" },
        "role": { "type": "string" },
        "part": { "type": "string" },
        "partKind": { "type": "string" },
        "itemId": { "type": "string" },
        "itemIndex": { "type": "integer", "minimum": 0 },
        "state": { "enum": ["focused", "hovered", "pressed", "selected", "disabled", "active"] },
        "label": { "type": "string" }
      },
      "additionalProperties": false
    },
    "frameHitTarget": {
      "type": "object",
      "required": ["id", "bounds"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "bounds": { "$ref": "#/$defs/rect" },
        "accepts": {
          "type": "array",
          "items": {
            "enum": ["pointerDown", "pointerUp", "click", "contextMenu", "scroll", "dragStart", "drag", "dragEnd", "hover", "enter", "leave"]
          },
          "uniqueItems": true
        },
        "focus": { "$ref": "#/$defs/pointerFocus" },
        "cursor": { "enum": ["pointer", "text", "default"] },
        "zIndex": { "type": "integer" }
      },
      "additionalProperties": false
    },
    "pointerFocus": {
      "oneOf": [
        {
          "type": "object",
          "required": ["kind", "path"],
          "properties": {
            "kind": { "const": "focus" },
            "path": { "type": "array", "items": { "type": "string" }, "minItems": 1 }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": ["kind"],
          "properties": { "kind": { "const": "preserve" } },
          "additionalProperties": false
        }
      ]
    },
    "frameBufferSnapshotMetadata": {
      "type": "object",
      "required": ["writtenBounds", "clearedBounds", "rowFingerprints", "fingerprint"],
      "properties": {
        "writtenBounds": { "$ref": "#/$defs/dirtyRegionSet" },
        "clearedBounds": { "$ref": "#/$defs/dirtyRegionSet" },
        "rowFingerprints": {
          "type": "array",
          "items": { "$ref": "#/$defs/frameRowFingerprint" }
        },
        "fingerprint": { "type": "string", "minLength": 1 }
      },
      "additionalProperties": false
    },
    "dirtyRegionSet": {
      "type": "object",
      "required": ["rects"],
      "properties": {
        "rects": {
          "type": "array",
          "items": { "$ref": "#/$defs/rect" }
        }
      },
      "additionalProperties": false
    },
    "frameRowFingerprint": {
      "type": "object",
      "required": ["row", "fingerprint"],
      "properties": {
        "row": { "type": "integer", "minimum": 1 },
        "fingerprint": { "type": "string", "minLength": 1 }
      },
      "additionalProperties": false
    },
    "rect": {
      "type": "object",
      "required": ["row", "column", "width", "height"],
      "properties": {
        "row": { "type": "integer", "minimum": 1 },
        "column": { "type": "integer", "minimum": 1 },
        "width": { "type": "integer", "minimum": 0 },
        "height": { "type": "integer", "minimum": 0 }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
